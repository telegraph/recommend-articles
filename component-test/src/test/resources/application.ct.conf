
app.host="localhost"
app.host=${?SERVICE_HOST}
app.port=9000
app.port=${?SERVICE_PORT}
app.name="recommend-articles"
app.baseUrl="http://"${app.host}":"${app.port}

mock.host="localhost"
mock.host=${?WIREMOCK_HOST}
mock.port=8080
mock.port=${?WIREMOCK_PORT}
mock.baseUrl="http://"${mock.host}":"${mock.port}

storage.host=${mock.host}
storage.port=${mock.port}
storage.baseUrl="http://"${storage.host}":"${storage.port}"/ucm-storage-service"
storage.name= "UCM Storage Client"

recommender.host=${mock.host}
recommender.port=${mock.port}
recommender.baseUrl="http://"${recommender.host}":"${recommender.port}"/recommender"
recommender.name= "Recommender Client"

